# 自动化任务：文献调研-数据整合（任务A）

请严格执行以下数据整合任务。

> **输入文件**：
> - `D:\Baek04w\待做\每周文献调研(1)\每周文献调研\中间结果\step01a-merged-YYYY-WXX.md`
> - `D:\Baek04w\待做\每周文献调研(1)\每周文献调研\中间结果\step01b-merged-YYYY-WXX.md`
> - `D:\Baek04w\待做\每周文献调研(1)\每周文献调研\中间结果\step02a-merged-YYYY-WXX.md`
> - `D:\Baek04w\待做\每周文献调研(1)\每周文献调研\中间结果\step02b-merged-YYYY-WXX.md`
> **输出文件**：
> - `D:\Baek04w\待做\每周文献调研(1)\每周文献调研\中间结果\step03-merged-YYYY-WXX.md`（合并去重后数据）
> **关联任务**：
> - auto-03b（分类评级+输出）应在本任务完成后执行

---

## 第一步：计算ISO周编号

请根据当前日期计算ISO周编号：
- 上周一 = 今天往前推到今天所在的周一，再往前推7天
- 确定上周的ISO周编号：YYYY-WXX

---

## 第二步：读取输入文件

请读取以下四个文件：

1. `D:\Baek04w\待做\每周文献调研(1)\每周文献调研\中间结果\step01a-merged-YYYY-WXX.md`
2. `D:\Baek04w\待做\每周文献调研(1)\每周文献调研\中间结果\step01b-merged-YYYY-WXX.md`
3. `D:\Baek04w\待做\每周文献调研(1)\每周文献调研\中间结果\step02a-merged-YYYY-WXX.md`
4. `D:\Baek04w\待做\每周文献调研(1)\每周文献调研\中间结果\step02b-merged-YYYY-WXX.md`

（YYYY-WXX替换为第一步计算出的ISO周编号）

**读取输入后检查**：
1. 逐个检查4个文件是否存在
2. 若某文件不存在 → 在输出文件头部的「缺失输入」行标注该文件名
3. 若3个及以上文件不存在 → 停止执行，输出错误信息

---

## 第三步：读取期刊缓存

读取 `D:\Baek04w\待做\每周文献调研(1)\每周文献调研\中间结果\journal-cache\` 文件夹：

- 若文件夹存在 → 遍历文件夹，将所有 `.md` 文件（以DOI命名）的数据加载为期刊缓存
- 若文件夹不存在 → 跳过，全部从WebSearch查询

**文件夹结构**：
```
D:\Baek04w\待做\每周文献调研(1)\每周文献调研\中间结果\journal-cache\
├── 10.1038_nbt.1234.md    ← DOI编号命名的缓存文件
└── ...
```

**单个缓存文件格式**：
```markdown
# 期刊缓存 - 10.1038/nbt.1234

| 字段 | 值 |
|------|---|
| 期刊名 | Nature Biotechnology |
| JCR分区 | Q1 |
| 影响因子 | 46.9 |
| 更新日期 | 2026-04-28 |
```

**查询逻辑**：
1. 提取论文的DOI编号
2. 在 `journal-cache\` 文件夹中查找对应的 `.md` 文件
3. 若命中 → 直接使用缓存数据
4. 若未命中 → WebSearch查询

---

## 第四步：合并四组结果

将step01a、step01b、step02a和step02b的表格数据合并为一张大表：
- 保留所有列（标题、作者、期刊、发表日期、DOI、发表状态、摘要、论文关键词、核心发现、检索来源、关键词编号）
- 新增「检索组」列，标注来源（A1/A2/B1/B2）
- 合并时保留完整摘要，不做截取

**记录合并统计**：记录每个来源组的论文数量

---

## 第五步：跨组去重

**主键**：DOI

**规则**：
- 若两行DOI相同 → 保留信息更完整的那行
- 合并「检索来源」「关键词编号」「检索组」列（取并集，用`; `分隔）
- 例：同一论文被组A1（#1,#3）和组B2（#34）同时召回 → 合并后关键词编号为 `#1,#3,#34`，检索组为 `A1;B2`

**无DOI时的备选去重**：
- 基于「标题 + 第一作者 + 发表年份」判断
- 标题相似度 ≥90% 视为重复
- 预印本和已发表版本：若标题相同且作者高度重合，视为重复，保留已发表版本

**核心发现合并**：若同一论文在不同检索组中生成了不同的核心发现描述，保留更详细完整的版本。

**去重后一致性抽检**：
- 从去重后的结果中随机抽取5条（不足5条则全部抽）
- 逐条检查：标题是否与摘要内容相关
- 若发现标题与摘要明显不匹配 → 在该条记录「备注」列标注"⚠️ 标题-摘要不一致"
- 抽检结果汇总在输出文件头部

---

## 第六步：保存中间结果

将合并去重后的数据保存为中间文件：

**输出文件**：`D:\Baek04w\待做\每周文献调研(1)\每周文献调研\中间结果\step03-merged-YYYY-WXX.md`

**文件格式**：
```markdown
## 合并去重结果

**ISO周编号**：YYYY-WXX
**输入**：组A1 [X] 条 + 组A2 [Y] 条 + 组B1 [Z] 条 + 组B2 [W] 条 = 合计 [T] 条
**去重后**：[V] 条

| # | 标题 | 作者 | 期刊 | 发表日期 | DOI | 发表状态 | 摘要 | 论文关键词 | 核心发现 | 检索来源 | 关键词编号 | 检索组 | 备注 |
|---|------|------|------|----------|-----|----------|------|-----------|---------|----------|-----------|--------|------|
```

**写入后确认**：文件成功写入，表格行数 > 0

---

## 执行完成确认

执行完成后，请输出：
```
✅ 数据整合完成（auto-03a）
- ISO周编号：YYYY-WXX
- 输入：组A1 [X] + 组A2 [Y] + 组B1 [Z] + 组B2 [W] = [T] 条
- 去重后：[V] 条
- 期刊缓存：命中 [N] / 未命中 [M]
- 一致性抽检：通过 [P] / 不通过 [Q]
- 输出文件：D:\Baek04w\待做\每周文献调研(1)\每周文献调研\中间结果\step03-merged-YYYY-WXX.md
- 后续任务：auto-03b（分类评级+输出）
```